# 孙振宇 SamSun 个人网站 · 视觉设置与叙事说明

- **文档版本**：v1.0（背景图全站化方案）
- **更新日期**：2026-08-29
- **适用范围**：`samsun-personal-site\` 全站 5 页
- **核心变更**：`hardcore-starship.jpeg` 由「首页 hero 装饰带（方案 A）」改为「**全站背景图**」，视觉系统由浅色主题反转并重构为深色玻璃拟态；叙事主线与背景图的星舰意象绑定。

---

## 一、背景图设置（全站统一）

### 1.1 技术实现（`assets/site.css`）

| 项 | 设置 | 说明 |
| --- | --- | --- |
| 挂载方式 | `body::before`（fixed 定位） | 全站所有页面、所有滚动位置共享同一背景 |
| 图片 | `url("images/bg-starship.jpg")`（相对 CSS 的 `assets/images/`，原图 2048×871） | **注意路径相对 CSS 文件而非 HTML** |
| 铺排 | `center 58% / cover no-repeat fixed` | 覆盖全屏；焦点锁定图片 58% 高度（视觉中心偏下，避开顶部导航区） |
| 滤镜 | `saturate(0.78)`（不再压亮度） | 压低饱和度落实「低饱和」（Q41），星舰主体亮度保持 |
| 蒙层 | `body::after` 三段式渐变（通透版） | 顶部 0.55（导航可读）→ 中部 0.30~0.34（星舰主体清晰透出）→ 底部 0.72（页脚收尾） |

### 1.2 图片实测参数（供视觉决策记录）

| 参数 | 值 |
| --- | --- |
| 尺寸 | 2048 × 871（≈21:9 超宽） |
| 平均色 | RGB 33 / 76 / 91（深蓝青） |
| 暗区占比（亮度<80） | **71.8%** |
| 亮区占比（亮度>175） | 3.8% |

**结论**：图片为深蓝青调暗色星舰图，天然适合深色全站背景；蒙层已调至通透档（顶部 0.55 / 中部 0.30 / 底部 0.72），保证星舰主体清晰可见的同时维持导航与正文对比度。

---

## 二、视觉设置（设计系统 v2，全站反转）

### 2.1 色彩 tokens（浅色 → 深色反转）

| Token | v1（浅色） | v2（深色全站背景） | 角色 |
| --- | --- | --- | --- |
| `paper` | `#F7F7F5` 冷纸白 | `#0E1418` 深墨 | 兜底色/文字反色 |
| `ink` | `#1C1E20` 石墨黑 | `#EAF0F2` 雾白 | 主文字（反转） |
| `ink-soft` | `#3A3F44` | `#AEBBC3` | 次级文字 |
| `steel` | `#5A6B78` | `#8FA9BB` 亮钢青 | 主强调（提亮保证对比） |
| `steel-soft` | `#7C8B96` | `#6E8799` | 弱强调 |
| `mist` | `#E4E9EC` | `rgba(255,255,255,0.08)` | 分隔/浅底 |
| `signal` | `#B98A2F` | `#D4A84F` 琥珀金 | 证据数字/结论（提亮） |
| `line` | `rgba(28,30,32,…)` | `rgba(234,240,242,0.16)` | 边框（反色） |

规则不变：一个色温（冷青灰）、一种主强调（钢青）、一种证据强调（琥珀金）；无高饱和、无渐变炫技。

### 2.2 玻璃拟态（新增，替代纯色卡片）

所有内容容器（卡片、观点条、引用块、路线入口、文章列表、联系卡等）改为：

```
background: rgba(15, 22, 28, 0.55);   /* 半透明深墨 */
backdrop-filter: blur(6px);            /* 背景图透出但被虚化 */
border: 1px solid rgba(234,240,242,0.09);
```

**作用**：全站背景图透出形成「沉浸式星舰舱」氛围，同时保证正文区可读性；编辑/杂志感的留白通过卡片间距与雾白文字维持。

### 2.3 字体（不变）

- 标题：思源宋体 / Noto Serif SC（600）
- 正文：思源黑体 / Noto Sans SC（400）
- 数字/标签/术语：IBM Plex Mono / Space Grotesk

### 2.4 动效（不变）

滚动淡入、时间线生长、数字计数；全部尊重 `prefers-reduced-motion`。

### 2.5 区块调整

| 区块 | v1 | v2 |
| --- | --- | --- |
| hero | 纸白底 + 底部 21:9 装饰带 | 直接叠于全站背景；hero 底部新增「星舰签名行」（HARD-CORE STARSHIP // FIELD FIRST · 现场/真实/验收） |
| 首页记忆点 | 纸白底 + 琥珀金左边线 | 玻璃卡 + 琥珀金左边线（同上） |
| 联系带（contact-band） | 纯深墨大色块 | 透明玻璃面板 + 顶部细线，背景图透出 |
| 页脚 | 浅色 | 深墨半透明 + 毛玻璃 |

---

## 三、叙事说明（背景图如何参与叙事）

### 3.1 星舰作为全站视觉隐喻

`hardcore-starship`（硬核星舰）承载三条叙事线，与 SamSun 的真实表达一一对应：

| 星舰隐喻 | SamSun 叙事 | 素材出处 |
| --- | --- | --- |
| **驶向深空 = 进入未知现场** | 长期探索 AI+X，进入真实问题，理解现场怎么运转 | Q2、Q47 |
| **硬核工程 = 真实交付** | 把 AI 做成真正有人能用、能够验收的东西；Demo 不是终点 | Q52-3、Q47 |
| **远航不靠围观 = 真实结果** | 「热闹」不等于真实成果；产品要有人使用、能验收、产生价值 | Q6、Q48-1 |

### 3.2 首页叙事节奏（全站背景下的阅读动线）

1. **首屏**：姓名 + 身份行 + 「我解决什么问题」记忆点（琥珀金高亮），直接压在全站星舰背景上——第一眼即「我是谁 + 我解决什么问题」（Q11）。
2. **星舰签名行**（hero 底部）：`HARD-CORE STARSHIP // FIELD FIRST` + `现场 · 真实 · 验收`——用背景图点名叙事主题。
3. **向下滚动**：转折故事（从「热闹」到「验收」）→ 三条路线 → 经历速览 → 实证成事循环 → 数字证据 → 联系。背景图全程透出，随内容区密度由蒙层控制对比。

### 3.3 星舰在叙事中的纪律（克制原则）

- 背景图**不叠加任何动态效果**（无视差、无缩放动画）——落实 Q42「轻微动效即可」与 Q40「前沿但不浮夸」。
- 星舰意象**只在首页签名行出现一次文字点名**，其余页面靠背景氛围含蓄呈现，不做重复说教。
- 背景图**不遮挡正文**：所有内容区有玻璃底或深墨蒙层保证可读性，满足无障碍对比度要求。

---

## 四、验证记录

| 检查项 | 结果 |
| --- | --- |
| 全站 5 页背景一致（fixed cover） | ✅ Chrome 151 实测 |
| 无横向溢出（桌面 + 390px 移动） | ✅ |
| 导航/正文/卡片对比度（深底雾白字） | ✅ |
| 滚动时背景固定、内容滚动 | ✅ |
| `prefers-reduced-motion` 关闭全部动效 | ✅ |
| 引用/资源无 404 | ✅ |

---

## 五、待确认（视觉相关）

1. **背景图饱和度**：当前 `saturate(0.78)` 为默认值，如觉得星舰色彩过浓或过淡可微调（0.6~0.9 区间）。
2. **蒙层强度**：当前通透档（0.55/0.30/0.72），如觉得星舰太亮影响正文可读可加深（上限 0.65/0.45/0.82）。
3. **焦点位置**：背景图固定于 `58%` 高度，如更希望突出星舰船体或星空区域可调整。
4. **背景图版权/来源**：确认可公开使用（上站前）。
